name: agent-memory
description: |
  This repository provides a reliable way to keep track of long‑running work
  and the decisions behind it. The system is fully self-describing and requires
  no prior context to operate correctly.

metadata:
  author: user
  version: 1.0.0
  deterministic: true
  append_only: true
  fail_closed: true
